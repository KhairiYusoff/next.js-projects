'use client'

import React from 'react'

const AddToCart = () => {
  return (
    <div>
        <button onClick={()=> console.log("clicked!")}>Add Card</button>
    </div>
  )
}

export default AddToCart